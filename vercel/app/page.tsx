export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Quat 2.0 AI Lab</h1>
      <p>Control plane skeleton (UI/API). GPU compute runs in <code>agent/</code> locally.</p>
    </main>
  );
}
