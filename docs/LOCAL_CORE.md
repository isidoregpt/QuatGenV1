# Local Core (Windows 11 + WSL2)

Recommended runtime:
- Windows 11 host
- WSL2 Ubuntu 22.04
- NVIDIA Windows driver with WSL CUDA support
- PyTorch (CUDA), RDKit, and RL stack inside WSL

Vercel/Firebase act as the control plane; the GPU loop runs locally.
