# Security / Safety

- API keys are stored in `.env` and never committed.
- Agent uploads only summaries (canonical SMILES + scores + metadata).
- This project is for *virtual design and scoring* and intentionally does not include real-world synthesis instructions.
